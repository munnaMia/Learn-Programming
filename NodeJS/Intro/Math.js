const sum = (a, b) => a + b;
const sub = (a, b) => a - b;
const mul = (a, b) => a * b;
const div = (a, b) => a / b;

// module.exports.div = div;
module.exports = { sum, sub, mul, div };

// console.log(module); // here module means the file data
